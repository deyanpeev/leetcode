const LETTERS = {
  '2': ['a', 'b', 'c'],
  '3': ['d', 'e', 'f'],
  '4': ['g', 'h', 'i'],
  '5': ['j', 'k', 'l'],
  '6': ['m', 'n', 'o'],
  '7': ['p', 'q', 'e', 's'],
  '8': ['t', 'u', 'v'],
  '9': ['w', 'x', 'y', 'z'],
}
function letterCombinations(digits: string): string[] {
  const result = [];
  for(let i = 0; i < digits.length; i++) {

  }

  return result;
};

function recursiveCombinations(): string[] {
  
}

console.log(letterCombinations('ab'));