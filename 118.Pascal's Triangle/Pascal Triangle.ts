function generate(numRows: number): number[][] {
  const result: number[][] = [];
  for(let i = 1; i <= numRows; i++) {
    let currentArr: number[] = [];
    for(let j = 1; j <= i; j++) {
      
    }
  }

  return result;
};